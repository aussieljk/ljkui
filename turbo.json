{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "ljkui#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "styles.css", "styles.lite.css", "theme.css"]
    },
    "build:css": {
      "dependsOn": ["^build"],
      "outputs": ["styles.css", "styles.lite.css", "theme.css"]
    },
    "lint": {
      "outputs": []
    },
    "lint:css": {
      "outputs": []
    },
    "@ljkui/docs#build": {
      "dependsOn": ["^build"],
      "outputs": [".vercel/output/**", ".nitro/**", ".tanstack/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
