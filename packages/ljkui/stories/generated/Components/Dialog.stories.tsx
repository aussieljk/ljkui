// GENERATED by scripts/generate-storybook.ts — do not edit.
import type { Meta, StoryObj } from '@storybook/react-vite';
import { examples } from '../../../examples/dialog.examples';
import { render } from '../../render-example';

const meta = {
  title: 'Components/Dialog',
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component:
          'Examples for `Dialog`, from examples/dialog.examples.tsx.\n\n## Keyboard\n\n| Key | Action |\n| --- | --- |\n| `Esc` | Close the dialog. |\n| `Tab` / `Shift+Tab` | Cycle focus within the dialog (focus is trapped). |\n\n## Accessibility\n\n- Rendered as `role="dialog"` with `aria-modal="true"`; the rest of the page is inert while open.\n- Focus moves to the dialog on open and returns to the trigger on close.\n- Give it an accessible name via the title, or `aria-label` when there is no visible title.',
      },
    },
  },
  argTypes: {
    size: {
      description:
        'The size of the dialog: controls its max width, padding and radius, and scales the default `Title` and `Description` sizes.',
      table: { type: { summary: '"1" | "2" | "3" | "4"' }, defaultValue: { summary: '3' } },
      control: false,
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  name: 'Default',
  render: () => render(examples['Default']),
};

export const Sizes: Story = {
  name: 'Sizes',
  render: () => render(examples['Sizes']),
};

export const WithInsetContent: Story = {
  name: 'With inset content',
  render: () => render(examples['With inset content']),
};

export const DetachedTriggers: Story = {
  name: 'Detached Triggers',
  render: () => render(examples['Detached Triggers']),
};

export const MultipleTriggersWithPayload: Story = {
  name: 'Multiple Triggers with Payload',
  render: () => render(examples['Multiple Triggers with Payload']),
};

export const ControlledMode: Story = {
  name: 'Controlled Mode',
  render: () => render(examples['Controlled Mode']),
};

export const ActionsRef: Story = {
  name: 'Actions Ref',
  render: () => render(examples['Actions Ref']),
};

export const InitialFocus: Story = {
  name: 'Initial Focus',
  render: () => render(examples['Initial Focus']),
};

export const FinalFocus: Story = {
  name: 'Final Focus',
  render: () => render(examples['Final Focus']),
};

export const OpenChangeCallbacks: Story = {
  name: 'Open Change Callbacks',
  render: () => render(examples['Open Change Callbacks']),
};

export const NestedDialogs: Story = {
  name: 'Nested Dialogs',
  render: () => render(examples['Nested Dialogs']),
};

export const CloseConfirmation: Story = {
  name: 'Close Confirmation',
  render: () => render(examples['Close Confirmation']),
};

export const WithPopoverAndHoverCard: Story = {
  name: 'With Popover and HoverCard',
  render: () => render(examples['With Popover and HoverCard']),
};

export const DialogFromDropdownMenu: Story = {
  name: 'Dialog from Dropdown Menu',
  render: () => render(examples['Dialog from Dropdown Menu']),
};

export const DialogTriggerInDropdownMenu: Story = {
  name: 'Dialog Trigger in Dropdown Menu',
  render: () => render(examples['Dialog Trigger in Dropdown Menu']),
};

export const DialogDetachedTriggerInDropdownMenuUsingHandle: Story = {
  name: 'Dialog detached trigger in Dropdown Menu using handle',
  render: () => render(examples['Dialog detached trigger in Dropdown Menu using handle']),
};
