// GENERATED by scripts/generate-storybook.ts — do not edit.
import type { Meta, StoryObj } from '@storybook/react-vite';
import { examples } from '../../../examples/alert.examples';
import { render } from '../../render-example';

const meta = {
  title: 'Components/Alert',
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component: 'Examples for `Alert`, from examples/alert.examples.tsx.',
      },
    },
  },
  argTypes: {
    color: {
      description:
        'The color of the alert, applied to its icon, title and description. Inherits the theme accent color when not set.',
      table: {
        type: {
          summary:
            '"danger" | "warning" | "success" | "info" | "red" | "orange" | "amber" | "yellow" | "lime" | "green" | "emerald" | "teal" | "cyan" | "sky" | "blue" | "indigo" | "violet" | "purple" | "fuchsia" | "pink" | "rose" | "slate" | "gray" | "zinc" | "neutral" | "stone"',
        },
      },
      control: false,
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  name: 'Default',
  render: () => render(examples['Default']),
};

export const Color: Story = {
  name: 'Color',
  render: () => render(examples['Color']),
};

export const SemanticColor: Story = {
  name: 'Semantic color',
  render: () => render(examples['Semantic color']),
};

export const ActionRenderProp: Story = {
  name: 'Action render prop',
  render: () => render(examples['Action render prop']),
};

export const ActionLoading: Story = {
  name: 'Action loading',
  render: () => render(examples['Action loading']),
};

export const ActionDisabled: Story = {
  name: 'Action disabled',
  render: () => render(examples['Action disabled']),
};

export const AsAlert: Story = {
  name: 'As Alert',
  render: () => render(examples['As Alert']),
};
