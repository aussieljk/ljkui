// GENERATED by scripts/generate-storybook.ts — do not edit.
import type { Meta, StoryObj } from '@storybook/react-vite';
import { examples } from '../../../examples/portal.examples';
import { render } from '../../render-example';

const meta = {
  title: 'Components/Portal',
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component: 'Examples for `Portal`, from examples/portal.examples.tsx.',
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  name: 'Default',
  render: () => render(examples['Default']),
};

export const CustomContainer: Story = {
  name: 'Custom Container',
  render: () => render(examples['Custom Container']),
};

export const WithRenderProp: Story = {
  name: 'With Render Prop',
  render: () => render(examples['With Render Prop']),
};

export const ModalExample: Story = {
  name: 'Modal Example',
  render: () => render(examples['Modal Example']),
};

export const ToastNotifications: Story = {
  name: 'Toast Notifications',
  render: () => render(examples['Toast Notifications']),
};
