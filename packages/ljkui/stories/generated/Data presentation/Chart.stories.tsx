// GENERATED by scripts/generate-storybook.ts — do not edit.
import type { Meta, StoryObj } from '@storybook/react-vite';
import { examples } from '../../../examples/chart.examples';
import { render } from '../../render-example';

const meta = {
  title: 'Data presentation/Chart',
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
    docs: {
      description: {
        component: 'Examples for `Chart`, from examples/chart.examples.tsx.',
      },
    },
  },
  argTypes: {
    data: {
      description:
        "Segments to render, in order. Each segment's width is its `value` as a percentage of the sum of all segment values. Every segment has a `label`, a numeric `value`, and a theme accent `color`.",
      table: { type: { summary: 'ChartData[]' } },
      required: true,
      control: false,
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  name: 'Default',
  render: () => render(examples['Default']),
};

export const CustomLabel: Story = {
  name: 'Custom label',
  render: () => render(examples['Custom label']),
};

export const Animated: Story = {
  name: 'Animated',
  render: () => render(examples['Animated']),
};
