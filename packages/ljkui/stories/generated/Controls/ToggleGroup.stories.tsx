// GENERATED by scripts/generate-storybook.ts — do not edit.
import type { Meta, StoryObj } from '@storybook/react-vite';
import { examples } from '../../../examples/toggle-group.examples';
import { render } from '../../render-example';

const meta = {
  title: 'Controls/ToggleGroup',
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component:
          'Examples for `ToggleGroup`, from examples/toggle-group.examples.tsx.\n\n## Keyboard\n\n| Key | Action |\n| --- | --- |\n| `←` / `→` | Move between items. |\n| `Space` / `Enter` | Toggle the focused item. |\n\n## Accessibility\n\n- Single tab stop with a roving tabindex; single- or multiple-selection via `aria-pressed`.',
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  name: 'Default',
  render: () => render(examples['Default']),
};

export const InADrawer: Story = {
  name: 'In a drawer',
  render: () => render(examples['In a drawer']),
};

export const ActivateOnFocus: Story = {
  name: 'Activate on Focus',
  render: () => render(examples['Activate on Focus']),
};

export const KeepMounted: Story = {
  name: 'Keep Mounted',
  render: () => render(examples['Keep Mounted']),
};
